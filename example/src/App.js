import {Button} from 'semantic-ui-react'
export function App() {
  return (
    <div>
      <h1>Hello world!</h1>
      <Button primary>This button is red because the primaryColor was changed in site.variables</Button>
    </div>

  )
}
